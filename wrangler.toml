name = "mesdeparts-ch-legacy-archive"
main = "cloudflare-worker/worker.js"
compatibility_date = "2025-12-17"

# Archive only: active Cloudflare deploy config lives in realtime_api/edge/wrangler.toml.
# Keep this file for historical reference; do not deploy it.

[vars]
RT_BACKEND_ORIGIN = "https://mesdeparts-ch.fly.dev"
